var paths = require('./paths');

var plugin = paths.plugin;

module.exports = {
    jquery: 'jquery/dist/jquery.min.js',
    src:paths.src,
	dist:paths.dist,
	page:paths.page,
	scss:paths.scss
};
