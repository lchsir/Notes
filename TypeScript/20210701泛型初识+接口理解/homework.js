window.onload = function () {
    var a = 1;
    arr = [1, 2, 3, 'a', 'b', 6];
    var arr = [1, 2, 3, 'a', 'b', 6];
    function Sum(a, b) {
        if (a === void 0) { a = 1; }
        if (b === void 0) { b = 1; }
        return a + b;
    }
    var fn = function (a, b) {
        var index = a.search(b);
        return index > -1;
    };
    fn('1', '21');

    
    
};
